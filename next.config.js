/**
 * @format
 * @type {import('next').NextConfig}
 */

const nextConfig = {
  reactStrictMode: true,
  basePath: '/Kyles-Cookbook',
  env: {
    basePath: '/Kyles-Cookbook',
  },
};

module.exports = nextConfig;
